import React from 'react'
import { View } from 'react-native'
import { containers } from '../styles/containers'

const CreateSpell = () => {
  return (
    <View style={containers.page}>
      <View style={containers.content}></View>
    </View>
  )
}

export default CreateSpell
